[package]
name = "eureka"
version = "0.1.0"
edition = "2021"
description = "Eureka chess engine: traditional alpha-beta + NNUE-in-progress."

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
