[package]
name = "chess-engine-demo"
version = "0.1.1"
edition = "2021"
description = "A small but correct traditional Alpha-Beta chess engine (Rust learning project). Phase-by-phase per the dev roadmap."

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
