//! Transposition table — implemented in Phase 5 (Zobrist hashing + TT).
//! Placeholder so the module layout matches the roadmap.

#[derive(Default)]
pub struct TranspositionTable {
    // TODO: Vec<TTEntry> keyed by Zobrist hash (Phase 5).
    // Bounds: Exact / LowerBound / UpperBound.
}
