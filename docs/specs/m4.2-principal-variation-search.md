# M4.2 — Principal Variation Search (PVS / NegaScout)

**Status:** DRAFT
**Base:** `fe4d346` (M4.1 closeout, `chore: close M4.1 quiet move ordering`)
**Goal:** Further reduce fixed-depth node count via Principal Variation Search
(null-window / NegaScout), **without** changing evaluation, search depth,
legal-move set, draw / mate / terminal semantics, TT key / mate codec /
replacement rules, killer / history formulas, the qsearch algorithm, or the
UCI surface.

M4.2 is **independent** of M4.0, M4.1, and every later search
milestone. It MUST be specified, accepted, and closed out on its own.
No other search algorithm (aspiration, SEE, LMR, null move, futility,
razoring, extensions, reductions, incremental eval, bitboard, multithread,
tapered / mop-up eval, Syzygy) may be bundled into this milestone's
commits or its Goal.

---

## 0. Formal starting point

Base: `fe4d346` (`chore: close M4.1 quiet move ordering`).

Already closed:

- M4.0 — Search Measurement Baseline
- M4.1 — Killer Moves + History Heuristic

M4.2 implements only:

- Principal Variation Search
- PVS / NegaScout-style null-window search

Goal: further reduce fixed-depth node count. Unchanged:

- evaluation
- search depth
- legal-move set
- draw / mate / terminal semantics
- TT key, mate codec, replacement rule
- killer / history formulas
- qsearch algorithm
- UCI surface

---

## 1. Core design: keep three SearchProfile generations

The existing two profiles are insufficient for a trustworthy M4.2 A/B,
because:

```
M4Reference = M4.0
Current     = M4.1
```

Once `Current` enables PVS, the directly-replayable M4.1 baseline is lost.

Therefore add:

```rust
// src/engine/search.rs (pub(crate))
pub(crate) enum SearchProfile {
    M4Reference,
    M41Reference,
    Current,
}
```

Precise definitions:

```
M4Reference
    M4.0 full-window alpha-beta
    no killer / history
    preserves the 1149 / 963 / 498,474 historical baseline

M41Reference
    M4.1 full-window alpha-beta
    uses killer / history
    no PVS
    preserves the 236,418-node M4.1 A/B baseline

Current
    M4.1 killer / history
    + M4.2 PVS
    UCI production path continues to use this profile
```

Implementation rules:

- `SearchHeuristics` is created by `M41Reference` and `Current`.
- The M4.1 seven-level ordering is shared by `M41Reference` and `Current`.
- PVS is enabled ONLY in `Current`.
- `M4Reference` MUST NOT carry any M4.1 / M4.2 per-node overhead.
- The original entry `search_best_move_with_history_and_tt()` keeps using
  `M4Reference` exclusively.
- UCI still calls the profile-aware entry with `Current`.
- No new UCI option.

---

## 2. PVS window contract

For the parent window:

```
[alpha, beta]
```

### 2.1 First move

The first ordered move MUST use the full window:

```
child window = [-beta, -alpha]
```

### 2.2 Later moves

Later moves first use a null window:

```
parent scout window = [alpha, alpha + 1]
child scout window  = [-(alpha + 1), -alpha]
```

A scout is used ONLY when ALL of the following hold:

```
profile == Current
not the first move at this node
alpha + 1 < beta
child is not a probe-confirmed terminal / intended-claim exact result
```

A safe addition MUST be used:

```rust
let Some(scout_beta) = alpha.checked_add(1) else {
    // cannot form a safe narrow window; fall back to the full window
};
```

If:

```
scout_beta >= beta
```

then the caller itself is already a null-window node; search it once with
the caller's original window and do NOT manufacture a "narrower window".

Never use a raw `alpha + 1` that could overflow.

---

## 3. Re-search conditions

After scouting a later move, the parent-view score is `score`.

### No re-search: fail-low

```
score <= alpha
```

The move cannot improve the current best value.

### Full re-search: improves alpha but does not cutoff

```
alpha < score < beta
```

Use the original effective window saved before this move was searched:

```
child full window = [-beta, -alpha_before_move]
```

The full re-search result is the move's final score and PV source.

### No re-search: fail-high

```
score >= beta
```

The scout already proves the beta cutoff holds; no full re-search needed.

A legal lower-bound score returned by the scout may be used to complete
the cutoff.

### Update timing

The following operations MUST each be performed exactly once, based only
on the move's FINAL search result:

- `best` update
- `alpha` update
- `node_best_move` update
- `pv.set_from_child()`
- killer update
- history update
- beta-cutoff decision

Must NOT:

- update once after the scout improves alpha, then update a second time
  after the re-search;
- reward history twice for the same quiet cutoff;
- commit the soon-to-be-fully-researched scout PV into the parent PV.

### 3.1 Heuristic state is not rolled back

The "update exactly once" rule above is re-scoped to the CURRENT parent
handling the CURRENT move:

```
when the current parent processes the current move:
    best
    alpha
    node_best_move
    parent PV
    the current parent's killer / history cutoff reward
    cutoff decision
are each submitted exactly once, based on that move's FINAL result.
```

This MUST NOT be interpreted as rolling back heuristic learning inside the
scout subtree. Locked:

- deep killer / history updates produced by a completed scout search are
  RETAINED;
- `SearchHeuristics` is NOT snapshotted, cloned, or rolled back before a
  scout;
- a full re-search MAY update the deep nodes it actually visits again;
- these are different real node visits.

---

## 4. Non-root negamax integration

PVS is first applied to:

```
depth > 0
non-root ordinary negamax
profile == Current
```

Keep the current seven-level ordering unchanged:

```
TT move
promotions
captures / en passant
killer 0
killer 1
history quiets
deterministic tie-break
```

The first move is produced by the order above and searched with the full
window; remaining moves follow the PVS rule.

### 4.1 ChildProbe

The first search of a move continues to use the current:

```
probe_child_draw()
```

It is responsible for:

- obtaining a precise node budget for a node;
- terminal child;
- intended fifty / threefold claim;
- child PV row cleanup.

If the first probe returns:

```
Terminal
IntendedClaim
```

the result is already exact; re-search is NOT allowed.

If it returns:

```
Continue
```

and the scout triggers a full re-search, then, while keeping the child
position and SearchPath already pushed:

1. obtain a real re-search node again via `try_enter_node()`;
2. call `negamax_entered_impl()`;
3. use the full window;
4. the researched node entry cleans up its own PV row;
5. on re-search abort, first restore path and position, then propagate
   `None` upward.

Every scout and every re-search is a real node visit and MUST each be
counted against the node budget.

### 4.2 Abort

An abort may occur at:

- before the scout starts;
- inside the scout;
- after the scout completes, before the re-search obtains a node;
- inside the re-search.

Each case MUST guarantee:

```
move is unmade
SearchPath is popped
Position / FEN / Zobrist fully restored
current partial iteration does not enter the final result
no partial parent TT entry is written
root keeps the previous completed iteration
```

Legal TT bounds written by an already-completed scout search may be kept;
what is forbidden is writing a pseudo-complete result for the not-yet-finished
parent node.

---

## 5. Root PVS

Root PVS is implemented independently of the non-root path, as a separate
commit.

Current root:

- uses the TT hash move and the previous-iteration best move;
- has no root beta cutoff;
- iterates over all root moves.

M4.2 keeps these behaviors.

### 5.1 Root rule

First root move:

```
full window
```

Later non-terminal root moves:

```
scout first
full re-search when score > alpha
```

Because the root beta is a safe +infinity sentinel and any normally
reachable score is below it, the root condition is equivalent to:

```
score > alpha → full re-search
```

but the code SHOULD still use the general condition:

```
score > alpha && score < beta
```

Terminal and intended-claim root moves are exact results; no re-search.

### 5.2 Root result exactness

After PVS, it is no longer required that "every failed root move gets an
exact score". What MUST be guaranteed:

- every fully-completed root iteration visits all root moves;
- fail-low root moves may keep only their upper-bound nature;
- any root move that could become the new best is fully re-searched;
- the final `RootIteration.score` is an exact score;
- except for the existing claimable-root 0-score fallback + empty-PV
  placeholder, the final bestmove and PV come from a full search or an
  exact terminal / intended-claim result;
- the root TT entry is still stored as Exact after the full iteration
  completes;
- the claimable-root 0-score floor is unchanged;
- no root beta cutoff is added.

The original "real scores for every root move" comment should be corrected
to:

```
all root moves are checked;
moves that may improve alpha are fully re-searched;
the final root best score is exact.
```

### 5.3 Root re-search node contract

Each root move first calls `probe_child_draw()` exactly once.

```
Terminal / IntendedClaim:
    exact result
    no scout, no re-search

Continue:
    first search uses the node already obtained from the probe
```

If a later root move's scout triggers a full re-search:

- the child position stays made;
- the SearchPath stays pushed;
- do NOT call `probe_child_draw()` again;
- call `try_enter_node()` exactly once to obtain the re-search node;
- on success call `negamax_entered_impl()` with the full window.

If the second node acquisition fails, or the full re-search aborts
internally:

- pop the SearchPath;
- unmake the root move;
- return `None`;
- the entire current root iteration is voided;
- the previous completed iteration is retained;
- no root Exact TT entry is written.

The scout and the full re-search are independent real node visits and
MUST each be counted against the node budget.
---

## 6. PV contract

The most common PVS correctness bug is not the score but residual PV.
Locked rules:

- scout fail-low and does not improve alpha: MUST NOT copy that child PV;
- scout fail-high causes cutoff: the cutoff move and its legal scout line
  may be recorded;
- scout triggers full re-search: finally only the child PV produced by
  the full re-search may be copied;
- the full re-search entry MUST clear the original scout child row;
- re-search abort: MUST NOT disguise the scout PV as a full PV;
- completed root PV MUST be stepwise-legal;
- a fallback move MUST NOT be disguised as a real PV;
- an empty-PV draw-claim placeholder remains allowed.

---

## 7. TT contract

MUST NOT modify:

```
TtKey
Exact / Lower / Upper meaning
mate-ply codec
depth-preferred replacement
hash-move legality check
TT capacity and UCI lifecycle
```

Current TT classifies bound by the caller's window; at equal depth an Exact
may replace a non-Exact. This rule is sufficient to support PVS's TT
contract: bound is classified relative to the caller window saved when the
node was entered, and the parent stores at most once — only after the node
completes normally or cuts off normally.

Locked:

- a scout search stores Upper / Lower / Exact by its scout call window;
- a full re-search reclassifies by its call window;
- the parent stores once, only after normal completion or normal cutoff;
- the parent bound is still classified relative to the caller window saved
  when the node was entered;
- when a draw floor raises the effective alpha, TT classification still
  uses the caller window from before the draw floor;
- an abort MUST NOT write a TT entry for the unfinished node;
- do NOT change the TT replacement strategy for PVS.

If implementation reveals a NEED to modify `tt.rs`, STOP immediately and
re-review the scope; do NOT modify it casually.

---

## 8. Draw, mate, and special results

MUST keep:

- terminal before draw;
- insufficient material auto 0;
- fifty-move and threefold are optional 0-score floors;
- intended-move claim exact 0;
- mate distance and sign unchanged;
- claimable root with no winning move returns 0-score fallback and empty PV;
- a claimable root where scout yields `score <= 0` needs no re-search;
- a claimable root where scout yields `score > 0` MUST be fully re-searched;
- qsearch implements NO PVS at all.

"qsearch implements no PVS" does NOT mean it always receives a full window:
a parent negamax scout may enter the depth-0 qsearch with a narrow window,
but qsearch's own algorithm, ordering, and recursion stay unchanged.

---

## 9. Test plan

### 9.1 Pure window unit test

Extract a small private pure function deciding:

```
Full
Scout { scout_beta }
```

Test:

- first move → Full;
- later move under `M41Reference` → Full;
- later move under `Current`, wide window → Scout;
- caller itself null-window → Full;
- `alpha` near `i32` upper bound → no overflow, fall back to Full;
- scout re-search condition:
  - `score <= alpha` → false;
  - `alpha < score < beta` → true;
  - `score >= beta` → false.

### 9.2 Profile tests

- `M4Reference` keeps the lock:
  - startpos d3 = 1149;
  - queen-win d3 = 963.
- `M41Reference` matches the final M4.1 implementation exactly.
- `M41Reference` and `Current` are identical before PVS lands.
- After PVS lands, do NOT lock `Current` node / bestmove / PV.
- UCI production profile is still `Current`.
- bench default is still `reference`.

### 9.3 Non-root PVS path

At least construct a fast position that really triggers:

```
later-move scout
scout improves alpha
full re-search
```

Use a test-only lightweight counter or private helper to assert both
branches actually happen; do NOT guess re-search ran merely from "node
count changed".

Also cover:

- scout fail-low with no re-search;
- scout fail-high direct cutoff;
- quiet fail-high records killer / history only once;
- tactical fail-high does not write killer / history;
- re-search PV comes from the full search.

### 9.4 Root PVS path

Cover at least:

- first root move full search;
- later root move fail-low with no re-search;
- later root move improves alpha and full re-search;
- all root moves still checked;
- root has no beta cutoff;
- root final score matches `M41Reference`;
- root final PV legal.

### 9.5 Correctness parity

Use fast fixtures comparing:

```
M41Reference vs Current
```

Include at least:

- startpos;
- queen-win or open-tactical;
- mate-in-1 / mate distance;
- insufficient material;
- current fifty-move claim;
- intended-move fifty-move claim;
- threefold with a REAL multi-key history;
- claimable root with a winning move;
- claimable root with no move exceeding 0;
- enabled-TT fixed depth;
- TT-disabled fixed depth.

Each requires:

```
score identical
mate distance identical
bestmove legal
PV stepwise-legal
Position / FEN / Zobrist / SearchPath restored
```

bestmove and PV MAY change when equal-score moves reorder.

### 9.6 Abort tests

Use a small node budget covering:

- scout abort;
- abort after scout completes, before re-search starts;
- re-search internal abort;
- depth-1 not completed;
- abort after a prior completed iteration exists.

Must verify:

```
node count does not exceed budget
exact hit when budget exhausted
stopped semantics correct
previous completed iteration retained
no partial parent TT store
Position and SearchPath restored
```

### 9.7 Default-test forbidden zone

Do NOT run in plain `cargo test`:

- full standard suite;
- 100k throughput;
- full warm suite;
- wall-clock thresholds;
- long self-play.

---

## 10. Benchmark profiles and commands

Commit 5 extends the bench CLI:

```
--profile reference
--profile m4.1
--profile current
```

Mapping:

```
reference → M4Reference
m4.1      → M41Reference
current    → Current
```

Default remains:

```
reference
```

Thus:

- M4.0 stays reproducible;
- M4.1 stays reproducible;
- M4.2 can run an in-the-same-binary A/B.

Append one compatibility note to the M4.1 benchmark report:

```
After M4.2 lands, the historical M4.1 Current result is replayed
with --profile m4.1. Original numbers and conclusions are unchanged.
```

Do NOT rewrite M4.1 historical data or label it a new measurement.

---

## 11. M4.2 Benchmark

New file:

```
docs/benchmarks/m4.2-principal-variation-search.md
```

### 11.1 Fixed-depth main measurement

#### Node & score gate (canonical aggregate)

```bash
cargo run --release -- bench standard \
  --mode disabled --profile reference --repeat 1

cargo run --release -- bench standard \
  --mode disabled --profile m4.1 --repeat 1

cargo run --release -- bench standard \
  --mode disabled --profile current --repeat 1
```

The **canonical aggregate** is defined as:

```
canonical aggregate
    = sum of node counts across the 10 fixtures
      in ONE standard-suite run
```

So the gate thresholds are read from the `--repeat 1` runs above:

```
reference = 498,474
m4.1     = 236,418
current  <= 224,597
```

Do NOT read the gate threshold from `bench_summary aggregate_nodes` of a
`--repeat 3` run, because the current harness sums all repeats.

#### Timing campaign

```bash
cargo run --release -- bench standard \
  --mode disabled --profile m4.1 --repeat 3

cargo run --release -- bench standard \
  --mode disabled --profile current --repeat 3
```

Definitions:

```
per-fixture median elapsed
    = median of that fixture's three elapsed_us samples

fixed-depth total median wall time
    = sum of the 10 per-fixture median elapsed_us
```

`Current`'s fixed-depth total median wall time MUST NOT be slower than
`M41Reference`'s (also restated in §12).

Record (timing campaign):

- per-fixture score;
- per-fixture nodes;
- node change and percentage;
- per-fixture median elapsed;
- aggregate / total wall time;
- bestmove / PV differences;
- all completeness checks.

#### M41Reference reproducibility

`M41Reference` MUST reproduce:

```
canonical aggregate nodes = 236,418
```

If it cannot, investigate a profile or behavior regression first; do NOT
proceed to explain PVS benefit.

The `--repeat 3` nodes MUST stay deterministic; you MAY report the
triple raw aggregate, but MUST NOT compare it directly against 236,418
or 224,597.

### 11.2 Fixed-node throughput (paired ratio gate)

**Original goal (unchanged):** detect whether `Current` introduces an
unacceptable per-node throughput regression relative to `M41Reference`.
The overall allowed drop is still **10%**.

#### 11.2.0 Why the original overall-median-of-9 metric was replaced

The original metric put **9 absolute NPS samples** from 3 fixtures with
very different intrinsic NPS ranges (`startpos`, `open-tactical`,
`queen-win`) into a single sequence and took its median. This is **not**
a fixture-paired estimator: its result can swing sharply depending on
where a fixture cluster lands in the sort order, rather than stably
representing the same-fixture `Current`-vs-`M41Reference` throughput
difference.

The pre-registered dual-order campaign at `ffd4109` produced Pair A
10.6% and Pair B -5.0%; the read-only NPS diagnostic further found
completed-depth differences (e.g. `startpos`: `Current` completes one
extra ply on the same 100k budget) and identical-search-output wall-time
block changes. Together these prove the old aggregation is unstable in
this environment.

This amendment does **not** re-interpret the old FAIL as a PASS. The old
NPS data (`c8b59c0` and `ffd4109`) is retained as **superseded
diagnostic evidence** in the benchmark report; it is **not** the new
paired gate. The new gate MUST pass on entirely fresh, forward-looking,
non-cherry-pickable data. See §11.2.9 for the historical-data handling.

#### 11.2.1 Bench-harness authorization (throughput-only fixture filter)

A later paired benchmark fix-forward MAY add a throughput-only fixture
filter to `src/engine/bench.rs`, e.g.:

```
--fixture startpos
--fixture open-tactical
--fixture queen-win
```

Constraints:

```
only selects which existing throughput fixture(s) run
MUST NOT modify any fixture FEN
MUST NOT modify the node budget
MUST NOT modify search dispatch
MUST NOT modify validation
MUST NOT modify the default profile
MUST NOT change standard / smoke behavior
```

A quick parser / filter test is allowed. This authorization applies ONLY
to the separate paired-benchmark fix-forward commit (§13), NOT to this
docs-only spec amendment.

#### 11.2.2 Paired campaign

For each throughput fixture, run independently:

```
node budget : 100000
TT mode     : disabled
1 warm-up pair (NOT counted toward results)
10 measured pairs
```

Each pair contains, for the SAME fixture:

```
one M41Reference run
one Current run
```

Order is strictly pre-registered and balanced:

```
AB BA AB BA AB BA AB BA AB BA

A = M41Reference
B = Current
```

i.e. 5 pairs `M41Reference → Current` and 5 pairs
`Current → M41Reference`. Each search uses `--repeat 1`. A single
profile's `--repeat 10` MUST NOT be substituted for paired execution.
All results are kept; post-hoc outlier deletion is forbidden.

#### 11.2.3 Affinity and environment

All measurement processes use:

```
same release binary
same working tree
same power mode
same pre-registered CPU affinity
strictly serial (no parallel build / test / bench)
no other agent heavy task
```

Because this machine is a hybrid-core CPU, affinity requirements:

```
bind to one fixed logical processor, or a fixed set of logical processors
fix it before the campaign starts
both profiles use exactly the same affinity
report the logical CPU number(s) or mask
do NOT claim it is a P-core unless verified
MUST NOT change affinity mid-campaign
```

#### 11.2.4 Pair ratio

Because every run searches exactly 100000 nodes, compute the ratio from
raw `elapsed_us` (avoids integer-NPS truncation):

```
pair_ratio
    = elapsed_us(M41Reference) / elapsed_us(Current)
    = Current NPS / M41Reference NPS
```

MUST use raw `elapsed_us`, NOT integer-rounded NPS.

```
pair_ratio = 1.00  → both sides identical
pair_ratio = 0.90  → Current throughput 10% lower
pair_ratio > 1.00  → Current faster
```

#### 11.2.5 Per-fixture metric

Sort each fixture's 10 pair_ratios; the fixture metric is the standard
even-sample median:

```
fixture_ratio
    = arithmetic mean of the 5th and 6th sorted pair_ratios
```

MUST use the standard even-sample median, NOT the current helper's
lower-median.

Report per fixture:

```
10 M41Reference elapsed_us
10 Current elapsed_us
10 pair_ratio
fixture_ratio
fixture_drop = 1 - fixture_ratio
each run's completed_depth
score
bestmove
PV length
stopped
nodes
execution order
```

#### 11.2.6 Overall metric

Three fixtures equally weighted (geometric mean of the fixture ratios):

```
overall_ratio
    = geometric_mean(
        startpos_fixture_ratio,
        open_tactical_fixture_ratio,
        queen_win_fixture_ratio
      )

overall_drop = 1 - overall_ratio
```

Hard gate (the 10% is NOT relaxed):

```
overall_ratio >= 0.90
```

i.e.:

```
overall paired NPS drop <= 10%
```

MUST NOT weight by absolute NPS magnitude, and MUST NOT reuse the
"overall median of 9 absolute NPS" design.

#### 11.2.7 Per-fixture guardrail

```
fixture_drop > 10%
    → MUST be explained

fixture_drop > 20%
    → blocks M4.2 closeout by default
```

A single-fixture regression above 20% MUST NOT be masked by improvement
in the other two fixtures.

#### 11.2.8 Order-stability check

Compute separately:

```
overall_ratio under AB order only
overall_ratio under BA order only
```

If:

```
abs(AB overall_drop - BA overall_drop) > 10 percentage points
```

then:

```
gate = FAIL / UNRESOLVED
```

closeout is NOT allowed even if the combined overall_ratio reaches 0.90.
The AB-only and BA-only aggregates are reported as stability evidence;
they are NOT independent primary gates.

#### 11.2.9 Campaign invalidation conditions

Only these objective events allow the WHOLE campaign to be voided and
re-run from scratch:

```
nodes != 100000
stopped != true
bench_error
command exits abnormally
affinity not applied as pre-registered
machine sleep or power loss
confirmed external heavy-task interruption
```

Forbidden:

```
deleting a single outlier
changing the order after seeing results
re-running only the failing profile
re-running only the failing fixture
cherry-picking the best of multiple campaigns
re-running identical code repeatedly until a PASS appears
```

#### 11.2.10 Historical data handling

All NPS data from `c8b59c0` and `ffd4109` stays in the M4.2 benchmark
report, labeled:

```
superseded diagnostic evidence
not the new paired gate
```

It MUST NOT be deleted, and the old FAIL MUST NOT be rewritten as a PASS.

### 11.3 TT recordings

```bash
cargo run --release -- bench standard \
  --mode cold --profile m4.1 --repeat 1

cargo run --release -- bench standard \
  --mode cold --profile current --repeat 1

cargo run --release -- bench standard \
  --mode warm --profile m4.1 --repeat 1

cargo run --release -- bench standard \
  --mode warm --profile current --repeat 1
```

cold / warm are recorded only; they are not the primary proof of PVS.

### 11.4 Historical baselines

MUST actually contain and keep running:

```bash
cargo run --release -- bench smoke
cargo run --release -- bench smoke --profile reference

cargo run --release -- bench standard \
  --mode disabled --profile reference --repeat 1

cargo run --release -- bench standard \
  --mode disabled --profile m4.1 --repeat 1

cargo run --release -- perft 5
```

MUST reproduce:

```
M4Reference smoke = 1149 / 963
M4Reference standard aggregate = 498,474
M41Reference standard aggregate = 236,418
perft(5) = 4,865,609
```

This is consistent with the canonical-aggregate gate in §11.1.

---

## 12. Performance acceptance

Primary comparison:

```
M41Reference vs Current
TT disabled
same fixture
same depth
same binary
same measurement campaign
```

### Hard gate

```
Current aggregate nodes
    at least 5% below 236,418
```

i.e.:

```
Current aggregate <= 224,597
```

Compute the minimum using the unrounded original numbers.

### Target

```
aggregate nodes reduced 10% or more
```

i.e. roughly:

```
Current aggregate <= 212,776
```

### Other gates

- all 10 fixture scores identical;
- mate distances identical;
- every fixed-depth search completes;
- Current fixed-depth total median wall time MUST NOT be slower than
  `M41Reference`;
- the fixed-node **paired throughput gate** (§11.2) MUST pass:
  `overall_ratio >= 0.90` (overall paired NPS drop <= 10%), no
  per-fixture `fixture_drop > 20%`, and no order-stability
  FAIL / UNRESOLVED. The original "overall median of 9 absolute NPS"
  gate is SUPERSEDED (see §11.2.0); `c8b59c0` / `ffd4109` NPS data is
  retained only as superseded diagnostic evidence;
- a single fixture whose node count increases by more than 20% MUST be
  explained;
- a single fixture whose node count increases by more than 30% blocks
  closeout by default, unless there is clear correctness AND total-time
  evidence;
- do NOT use warm-TT results to mask disabled-mode regression;
- do NOT substitute Elo or a few games for node and correctness gates.

If the aggregate node reduction is below 5%, M4.2 MUST NOT be closed on
a "PVS feature complete" pretext.

---

## 13. Commit split

### Commit file whitelist (Commits 2-4)

- Commit 2: `src/engine/search.rs` + `src/engine/bench.rs`
  - `bench.rs` may receive ONLY the minimal compile-compat required by
    the third enum variant:
    - add the `SearchProfile::M41Reference => "m4.1"` arm to the
      existing exhaustive `profile_str()` match;
    - sync any in-file comment that counts the number of profiles;
    - MAY add a quick `profile_str` mapping test in the existing
      `bench.rs` test module.
  - `bench.rs` MUST NOT in Commit 2:
    - let `parse_args` accept `--profile m4.1`;
    - modify bench help;
    - change the default profile;
    - change search-dispatch semantics;
    - change any fixture / lock / validation.
  - i.e. `profile_str(M41Reference)` compiles with a stable internal
    string, but user input `--profile m4.1` is still REJECTED in
    Commit 2 (real CLI exposure stays in Commit 5).
- Commit 3: ONLY `src/engine/search.rs` (tests only in its
  `#[cfg(test)]` module).
- Commit 4: ONLY `src/engine/search.rs` (tests only in its
  `#[cfg(test)]` module).
- No new test file is created for Commits 2-4:
  - Commit 2 search-profile tests live in the existing `search.rs`
    test module; the `profile_str` compat test MAY live in the existing
    `bench.rs` test module.
  - Commit 3 and Commit 4 tests live only in `search.rs`.
- Commit 5 and Commit 6 keep the whitelist already listed under their
  own subsections.

### Commit 1

```
docs: specify M4.2 principal variation search
```

Only:

```
docs/specs/m4.2-principal-variation-search.md
```

Push, then stop and await remote review.

### Commit 2

```
refactor(search): add M4.1 reference profile
```

Content:

- add `M41Reference`;
- `M4Reference` unchanged;
- `M41Reference` and `Current` both temporarily run the M4.1 full-window
  search;
- heuristics and seven-level ordering support both profiles;
- both create killer / history state and use the seven-level ordering;
- profile quick tests;
- implement NO PVS;
- UCI still passes `Current`;
- the legacy entry still passes `M4Reference`;
- bench default stays `reference`;
- `--profile m4.1` is NOT yet exposed (CLI exposure stays in Commit 5).

Files: `src/engine/search.rs` + `src/engine/bench.rs`; `bench.rs`
receives ONLY the minimal third-variant compile-compat (the `profile_str`
arm + comment sync + optional `profile_str` mapping test), and MUST NOT
accept `--profile m4.1`, change help / default / dispatch / fixtures /
locks / validation. Search-profile tests live in `search.rs`; the
`profile_str` compat test MAY live in the existing `bench.rs` test module.

Push, then stop.

### Commit 3

```
feat(search): add non-root principal variation search
```

Content:

- non-root ordinary negamax PVS;
- safe null-window;
- full re-search;
- abort / PV / TT / killer / history semantics;
- root keeps full window;
- qsearch unchanged.

Files: ONLY `src/engine/search.rs` (tests in its `#[cfg(test)]` module).

Push, then stop.

### Commit 4

```
feat(search): add root principal variation search
```

Content:

- root first-move full search;
- later-move scout;
- improvement full re-search;
- no root beta cutoff;
- root exact score / PV tests.

Files: ONLY `src/engine/search.rs` (tests in its `#[cfg(test)]` module).

Push, then stop.

### Commit 5

```
bench: record M4.2 PVS search deltas
```

Allowed:

```
src/engine/bench.rs
README.md
docs/benchmarks/m4.2-principal-variation-search.md
docs/benchmarks/m4.1-quiet-move-ordering.md
```

The M4.1 report may only receive an appended profile-compatibility note;
historical data is unchanged.

Push, then stop.

### Post-Commit-5 gate redesign (added after the `ffd4109` NPS FAIL)

The `ffd4109` evidence commit was reviewed APPROVED, but its honest data
left the fixed-node NPS gate at FAIL / UNRESOLVED (see §11.2.0). Before
Commit 6, two additional commits are inserted:

```
Gate spec amendment (this commit):
    docs/specs/m4.2-principal-variation-search.md only
    -> "docs: redesign M4.2 paired throughput gate"

Paired benchmark fix-forward (next commit, separately authorized):
    src/engine/bench.rs
    docs/benchmarks/m4.2-principal-variation-search.md
    -> runs the §11.2 paired-ratio campaign and records it
```

The paired benchmark fix-forward MUST pass an independent remote review
before Commit 6 is authorized. No search-implementation change is
authorized by this redesign; the gate is a methodology correction, not a
relaxation (the 10% overall drop limit is unchanged).

### Commit 6

```
chore: close M4.2 principal variation search
```

Only:

```
docs/specs/m4.2-principal-variation-search.md
docs/benchmarks/m4.2-principal-variation-search.md
```

Record:

- CLOSED;
- full lineage;
- nodes / NPS / wall-time results;
- correctness;
- limits;
- stop statement.

Push, then stop and await final remote review.

---

## 14. Per-commit gate

```bash
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test
cargo test --release
git diff --check
cargo run --release -- perft 5
```

Commit 5 additionally runs the full A/B commands.

All shell pipelines MUST preserve the real exit code; do NOT use forms where
`tail` / `grep` hide a cargo failure. When truncating output, use a
pipefail-aware approach, or save the cargo exit code first.

---

## 15. Explicitly forbidden

M4.2 MUST NOT contain:

```
aspiration windows
SEE
LMR
null move
futility pruning
razoring
extensions
reductions
qsearch PVS rewrite
killer / history tuning
root move-ordering rewrite
move-list rewrite
incremental evaluation
new evaluation feature
bitboard
multithreading
Syzygy
version bump
tag
```

MUST NOT modify:

```
src/chess/*
src/engine/eval.rs
src/engine/tt.rs
Cargo.toml
Cargo.lock
```

If PVS implementation is judged to require changing TT replacement, draw
rules, or qsearch, STOP and re-review the scope independently.

---

## 16. STOP condition

Per commit:

```
commit
fetch to confirm remote has not advanced
push
confirm HEAD == origin/main
working tree clean
stop immediately
await independent remote review
```

After Commit 6:

```
M4.2 still needs final remote APPROVED to be formally CLOSED.
```

Do NOT auto-start:

```
M4.3 aspiration
SEE
LMR
null move
any later search / evaluation / bitboard / multithread work
```

---

<!-- Closeout section filled on Commit 6 -->
