# D1.14 — Current vs CurrentLmr Elo/SPRT

Status: **SMOKE PASS — FORMAL SPRT NOT STARTED**

This milestone compares the approved production profile with the isolated
`current-lmr` candidate through Cutechess. It does not modify Rust search code,
LMR parameters, `Current`, evaluation, time management, TT, or move ordering.

## Fixed roles and provenance

```text
candidate / Cutechess player 1: CurrentLmr (--profile current-lmr)
baseline / Cutechess player 2: Current   (--profile current)
candidate result perspective:  CurrentLmr minus Current
approved engine-code baseline: dcb009813239549f4805e9a2467aadae4ccebc11
```

Both players use the same release binary. Only the startup profile argument
differs. The launcher probes both processes before starting a match and stores
the complete argv, binary SHA-256, Git SHA, UCI identity, manager identity and
UCI configuration in the manifest.

The opening suite is the committed
`tests/data/openings/d1.14-openings-v1.epd`: 4,852 positions selected from
official Stockfish Books at ref
`65815ccdbc7727cd4f6aee252ba8f67fb740e92f`. The archive, extracted-content
SHA-256 and selection metadata are pinned in the companion JSON manifest. The
source repository identifies this book as CC0-1.0.

Cutechess is pinned to 1.5.1. Its executable SHA-256 and the exact command are
recorded in each run artifact. Cutechess owns clocks, legal move handling,
pairing and SPRT; the Python code does not reimplement any of those systems.

## Protocol smoke

The committed smoke run uses:

```text
openings: first 20 committed positions
games: 40 (20 paired openings)
time control: 5+0.05
Hash: 16 MB
engine threads: 1
Cutechess concurrency: 1
opening order: sequential
opening policy: default, with repeat=2 and automatic color swap
draw/resign/tablebase adjudication: disabled
```

The `default` opening policy is intentional: `encounter` treats the same two
engines as one continuing encounter and can reuse the first EPD position.
`default` advances after the two color-swapped repetitions. The verifier checks
the resulting PGN rather than trusting the command line alone.

Artifact directory:

```text
results/d1.14/smoke/
```

The independent verifier confirms:

```text
games                         40 / 40
opening pairs                 20 / 20
color swap                    PASS
PGN legal replay              PASS
profile identities            Current / CurrentLmr
unexpected manager failures  0
time forfeits                 0
integrity status              PASS
```

The candidate-oriented descriptive sample is:

```text
CurrentLmr: 22 wins, 7 draws, 11 losses
score:      63.75%
descriptive estimate: +98.1 +/- 112.0 Elo (95% descriptive error only)
```

This is a protocol smoke result, not a strength conclusion. It is too small to
accept or reject a candidate and is not an SPRT decision.

## Formal match gate

The formal launcher profile is prepared but has not been started:

```text
openings: all 4,852 positions
games: 9,704 maximum (4,852 paired openings)
time control: 10+0.1
Hash: 16 MB
engine threads: 1
Cutechess concurrency: 2
SPRT: elo0=0, elo1=5, alpha=0.05, beta=0.05
```

Formal interpretation is deliberately delegated to Cutechess:

```text
H1 accepted -> PASS
H0 accepted -> REJECTED
maximum games without a boundary -> INCONCLUSIVE
any protocol/process/artifact failure -> INTEGRITY_FAIL
```

No formal match may start unless the output directory is new, the worktree is
clean, the engine-code baseline is unchanged, both UCI identity probes match
their requested profiles, and the opening/binary/manager hashes pass.

`Current` remains unchanged. A smoke pass does not authorize production
promotion, LMR parameter changes, or any other search optimization.
