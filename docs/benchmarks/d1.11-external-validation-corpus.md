# D1.11.1 — External Position-Slice Differential Smoke

Status: **PASS — DIFFERENTIAL SMOKE ONLY; MOTIF VALIDATION NOT ESTABLISHED**

Baseline: `313a1de`

Corpus commit: `cf651f3`

Runner commit: `53f1423`

Fix-forward: source-line verification and abnormal-exit hardening are kept in
the current working lineage after `0f4b6ed`.

## Scope

D1.11 adds a small, versioned external position slice derived from the
official Stockfish books repository. It provides independent differential
smoke evidence for the existing bench-only `current-qsearch-pruning` profile;
it does not change `Current`, SEE, qsearch, or any search control flow.

This corpus is separate from the 23-case project-curated D1.10 corpus:

| Corpus | Purpose | Cases | Source |
| --- | --- | ---: | --- |
| D1.10 | pinned regressions for known SEE/qsearch hazards | 23 | project-curated FENs |
| D1.11 | independent external position slice | 32 | official Stockfish books |

Neither corpus is an Elo suite, motif oracle, or complete tactical suite. A
passing run means only that the candidate did not regress the pinned process,
legality, PV, and relative-score checks at the recorded resource limit.

## Provenance and snapshot

The committed provenance file is:

```text
tests/data/external_validation_v1.sources.json
```

It pins:

- repository: `https://github.com/official-stockfish/books`;
- source ref: `65815ccdbc7727cd4f6aee252ba8f67fb740e92f`;
- license: `CC0-1.0`;
- each upstream archive URL;
- the SHA-384 of each raw extracted EPD content;
- the SHA-256 of the committed corpus snapshot:
  `e37e4657c4e3fc7657e9127b25ca0c109a9aefe9500f54004b7e3d35034e858e`;
- the exact source-line list for each slice.

The raw-content hashes describe the extracted EPD bytes, not the ZIP archive
bytes. The committed snapshot is the runnable, reviewable test input, while
the `source_line` field remains an audit pointer into the pinned upstream
file. Local book caches are not required to run the validation and remain
outside the repository.

The selection is a fixed source-line slice, not a claim that the upstream
books are motif-labeled tactical or promotion suites. The offline verifier
reads the prepared local source files, checks their raw SHA-384 values, and
compares every selected source line's complete FEN with the committed case.
Every case records its source book and source line in:

```text
tests/data/external_validation_v1.epd
```

The fixed split is:

| Slice | Upstream source | Cases | Depth |
| --- | --- | ---: | ---: |
| `closedpos` | `stockfish-closedpos` | 8 | 4 |
| `stalemate-stress` | `stockfish-stalemates-200d30-v1` | 8 | 4 |
| `endgames-a` | `stockfish-endgames` | 8 | 4 |
| `endgames-cdb` | `stockfish-endgames-cdb95105` | 8 | 4 |

These names intentionally describe source slices. The corpus contains no
answer moves, forced-mate oracle, promotion requirement, tablebase WDL/DTZ,
or other motif assertion. Therefore it must not be reported as four completed
tactical/mate/promotion/endgame validations.

## Runner and gates

The process-level runner is:

```text
tools/run_external_validation.py
```

It starts a fresh UCI process for each profile and case. Both profiles receive
the identical FEN, depth, Hash value and absolute wall-clock timeout:

```text
baseline:  --profile current
candidate: --profile current-qsearch-pruning
Hash:      16 MB
depth:     4 per case
timeout:   30 seconds per search
```

Before searching, the runner fails closed unless all of the following hold:

- the snapshot SHA-256 matches provenance;
- all four source entries match `books/manifest.json` for repository, ref,
  license, archive URL and raw-content hash;
- the corpus has exactly 32 unique cases and exactly 8 cases per group;
- every FEN is valid and every source line pointer is unique;
- both UCI processes report the expected startup profile.

For every real search it records the profile, case, source pointer, depth,
typed score, bestmove and PV. It then checks:

- completed depth is at least the case depth;
- non-terminal bestmove is legal;
- PV is non-empty, starts with bestmove and can be replayed fully as legal
  moves from the root FEN;
- `bestmove 0000` is accepted only for a real checkmate or stalemate;
- mate scores have a non-zero distance;
- process exit, stdout/stderr, timeout and cleanup remain healthy;
- the candidate score class is not below the Current baseline class.

Normal completion now also requires the child process to exit with code `0`.
If the search body already failed, cleanup remains best-effort so that the
original protocol or legality error is preserved. The Python tests exercise
non-zero exit, profile mismatch, illegal bestmove, empty/mismatched PV,
insufficient depth, continuous-output timeout and stderr diagnostics.

Every case is retained in the JSON report, including the role and diagnostic
context for a failure. The command returns only `PASS` or `FAIL`; it never
maps a missing search, timeout or integrity error to a candidate rejection.

The offline source-line verifier is:

```text
tools/build_external_validation_v1.py
```

Run it after the four pinned EPD files have been prepared under
`books/cache`; it does not access the network:

```bash
python tools/build_external_validation_v1.py
```

Run the process smoke against a release binary with:

```bash
cargo build --release
python tools/run_external_validation.py \
  --engine target/release/chess-engine-demo \
  --report results/d1.11-external-validation-v1.json
```

On Windows, use `target/release/chess-engine-demo.exe`.

## Recorded v1 result

The recorded local release run completed:

```text
decision:          PASS
cases:             32 / 32
searches:          64 / 64
closedpos:         8 / 8 passed
stalemate-stress:  8 / 8 passed
endgames-a:        8 / 8 passed
endgames-cdb:      8 / 8 passed
case failures:     0
```

This is an external position-slice differential smoke pass, not a completed
tactical/mate/promotion/endgame validation result and not a strength result.
It authorizes the next independent validation step only. It does not
authorize:

```text
Current promotion
D1.3 SEE pruning in Current
Elo or SPRT
Aspiration activation
```

The next evidence should increase independence and difficulty, for example
with higher-depth reruns, randomized differential sampling, or a broader
pinned corpus. Until then `current-qsearch-pruning` remains validation-only
and `Current` remains unchanged.
